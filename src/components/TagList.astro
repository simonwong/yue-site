---
import { cn } from "@/utils/utils";
import { badgeVariants } from "./ui/badge";
const { tags } = Astro.props
---

<div >
  {
    tags.map(tag => (
      <a class={cn(badgeVariants(), 'mr-2')} href={`/tags/${tag}`}>{tag}</a>
    ))
  }
</div>
